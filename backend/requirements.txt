Flask>=3.0,<4.0
Flask-Cors>=4.0,<5.0
python-dotenv>=1.0,<2.0
pytest>=8.0,<9.0

# Optional: only needed if GEMINI_API_KEY is set to enable the real
# AI classifier (services/classifier_service.py). The app runs fine
# without it -- classification just falls back to the deterministic
# rule-based classifier.
google-genai>=1.0,<3.0
